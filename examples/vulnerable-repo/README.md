# Vulnerable example (intentional)

Minimal repository layout used to demonstrate failing controls in OSS Policy Kit reports.

Use this fixture to validate CI gate behavior (`--fail-on fail`) and remediation messaging.
