# Generated by oss-policy-kit export-policy (ADR-035). DO NOT EDIT BY HAND.
# Regenerate: oss-policy-kit export-policy --profile github-level-1 --format cel
#
# FIDELITY BOUNDARY (read this):
#   Each expression checks the kit's OWN output. Bind the CEL variable
#   `report` to an oss-policy-kit evaluation-report.json on the reports/2.0
#   contract: {controls: [{id, state, ...}]}. These expressions assert that
#   every control this profile expects is present and passing; they do NOT
#   re-implement the kit's analysis. Validate semantics in your CEL host
#   (Kyverno, cel-go, etc.).
#
# Profile: github-level-1 — GitHub OSS starter baseline (level 1)
# Controls: 14 (sorted by id). One expression per control, then an aggregate.
# Each non-comment line is a standalone CEL boolean expression.

# CI-DANGER-007 — No pull_request_target without strong justification [assurance=deterministic]
report.controls.exists(c, c.id == "CI-DANGER-007" && c.state in ["PASS", "ATTESTED", "SELF_ATTESTED", "NOT_APPLICABLE"])

# CI-LEAST-009 — Workflow permissions are not obviously over-broad [assurance=signal]
report.controls.exists(c, c.id == "CI-LEAST-009" && c.state in ["PASS", "ATTESTED", "SELF_ATTESTED", "NOT_APPLICABLE"])

# CI-PERM-006 — Workflows declare explicit top-level permissions [assurance=deterministic]
report.controls.exists(c, c.id == "CI-PERM-006" && c.state in ["PASS", "ATTESTED", "SELF_ATTESTED", "NOT_APPLICABLE"])

# CI-PIN-008 — Third-party actions pinned to immutable references [assurance=deterministic]
report.controls.exists(c, c.id == "CI-PIN-008" && c.state in ["PASS", "ATTESTED", "SELF_ATTESTED", "NOT_APPLICABLE"])

# CI-WF-005 — GitHub Actions workflows exist [assurance=deterministic]
report.controls.exists(c, c.id == "CI-WF-005" && c.state in ["PASS", "ATTESTED", "SELF_ATTESTED", "NOT_APPLICABLE"])

# GOV-CON-002 — CONTRIBUTING guide present [assurance=deterministic]
report.controls.exists(c, c.id == "GOV-CON-002" && c.state in ["PASS", "ATTESTED", "SELF_ATTESTED", "NOT_APPLICABLE"])

# GOV-COWN-003 — CODEOWNERS configured [assurance=deterministic]
report.controls.exists(c, c.id == "GOV-COWN-003" && c.state in ["PASS", "ATTESTED", "SELF_ATTESTED", "NOT_APPLICABLE"])

# GOV-DISC-013 — Responsible disclosure channel documented [assurance=signal]
report.controls.exists(c, c.id == "GOV-DISC-013" && c.state in ["PASS", "ATTESTED", "SELF_ATTESTED", "NOT_APPLICABLE"])

# GOV-LIC-004 — LICENSE file present [assurance=deterministic]
report.controls.exists(c, c.id == "GOV-LIC-004" && c.state in ["PASS", "ATTESTED", "SELF_ATTESTED", "NOT_APPLICABLE"])

# GOV-SEC-001 — SECURITY.md present [assurance=deterministic]
report.controls.exists(c, c.id == "GOV-SEC-001" && c.state in ["PASS", "ATTESTED", "SELF_ATTESTED", "NOT_APPLICABLE"])

# GOV-WAIV-014 — Versioned waiver policy file present in repository (not CLI-only waivers) [assurance=deterministic]
report.controls.exists(c, c.id == "GOV-WAIV-014" && c.state in ["PASS", "ATTESTED", "SELF_ATTESTED", "NOT_APPLICABLE"])

# REL-CHANGE-012 — Changelog or documented release notes [assurance=deterministic]
report.controls.exists(c, c.id == "REL-CHANGE-012" && c.state in ["PASS", "ATTESTED", "SELF_ATTESTED", "NOT_APPLICABLE"])

# SEC-CODEQL-010 — CodeQL or equivalent security scanning in CI [assurance=signal]
report.controls.exists(c, c.id == "SEC-CODEQL-010" && c.state in ["PASS", "ATTESTED", "SELF_ATTESTED", "NOT_APPLICABLE"])

# SEC-DEPREV-011 — Dependency review in pull requests [assurance=deterministic]
report.controls.exists(c, c.id == "SEC-DEPREV-011" && c.state in ["PASS", "ATTESTED", "SELF_ATTESTED", "NOT_APPLICABLE"])

# ALL — every profile control present and in a passing state
["CI-DANGER-007", "CI-LEAST-009", "CI-PERM-006", "CI-PIN-008", "CI-WF-005", "GOV-CON-002", "GOV-COWN-003", "GOV-DISC-013", "GOV-LIC-004", "GOV-SEC-001", "GOV-WAIV-014", "REL-CHANGE-012", "SEC-CODEQL-010", "SEC-DEPREV-011"].all(id, report.controls.exists(c, c.id == id && c.state in ["PASS", "ATTESTED", "SELF_ATTESTED", "NOT_APPLICABLE"]))
